{
  "name": "exaryn-studio",
  "version": "1.0.0",
  "private": true,
  "description": "Exaryn — studio portfolio + daily AI signal digest. Pure static, no build step.",
  "scripts": {
    "dev": "npx -y serve -l 3000 .",
    "digest": "python scripts/digest.py"
  }
}
